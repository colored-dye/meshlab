#version 330
layout(location = 0) in vec3 aPos;

uniform mat4 view;
uniform mat4 projection;
// uniform mat4 rotation;
// uniform mat4 translation;
// uniform mat4 scale;
// uniform mat4 move;
uniform mat4 model;

void main()
{
  // gl_Position = projection * view * scale * move * rotation * translation * vec4(aPos, 1.0f);
  gl_Position = projection * view * model * vec4(aPos, 1.0f);
  gl_PointSize = 3.0f;
}
